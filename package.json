{
  "name": "dm-drawing-board",
  "version": "1.2.3",
  "private": true,
  "description": "A neubrutalist Electron and GitHub Pages toolkit for Dungeons & Dragons DMs.",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check electron/main.js && node --check electron/preload.js && node --check shared/security.js && node --check platform.js && node --check renderer.js && node --check scripts/build-pages.js",
    "test": "npm run check && node --test",
    "pages:build": "node scripts/build-pages.js",
    "audit": "npm audit"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "keywords": [
    "electron",
    "github-pages",
    "dnd",
    "dungeons-and-dragons",
    "worldbuilding",
    "dm-tools"
  ],
  "author": "KeyOuts",
  "license": "MIT",
  "devDependencies": {
    "electron": "43.2.0"
  }
}
